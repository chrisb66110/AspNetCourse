@model IEnumerable<AspNetCourse.Models.CarColor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.car.registrationNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.color.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.car.registrationNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.color.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { idCar = item.idCar, idColor = item.idColor }) |
            @Html.ActionLink("Details", "Details", new { idCar = item.idCar, idColor = item.idColor }) |
            @Html.ActionLink("Delete", "Delete", new { idCar = item.idCar, idColor = item.idColor })
        </td>
    </tr>
}

</table>
